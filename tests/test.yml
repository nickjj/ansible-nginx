---
- hosts: localhost
  remote_user: travis
  sudo: true
  vars:
    nginx_listen: 8080
    nginx_base_domain: localhost
    nginx_upstream_server: unix:///tmp/testproject.sock
    nginx_backend_name: testproject
    nginx_root_path: /tmp
    nginx_error_pages:

  roles:
    - ansible-nginx