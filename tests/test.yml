---
- hosts: localhost
  remote_user: travis
  sudo: true
  vars:
    nginx_base_domain: localhost
    nginx_upstream_server: /home/travis/testproject_puma/tmp/puma.sock
    nginx_root_path: /home/travis/testproject_puma/public

  roles:
    - ansible-nginx