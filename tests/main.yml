---

- hosts: all
  become: True

  vars:
    nginx_sites:
      default:
        domains: ['localhost']
        default_server: True
        listen_http: 8080
        listen_https: 8081

  roles:
    - ansible-nginx
