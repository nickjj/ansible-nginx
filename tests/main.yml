---

- hosts: all
  vars:
    nginx_sites:
      default:
        listen_http: 8080
        listen_https: 8081
        domains: ['localhost']

  pre_tasks:
    - name: Update APT cache
      apt:
        update_cache: True

  roles:
    - this_role
